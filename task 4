int sensorPIN=A0;
 int sensorValue;

 void setup(){

  Serial.begin(9600);
  pinMode(sensorPIN, INPUT);

  
 }
 void loop(){

  sensorValue= analogRead(sensorPIN);
  Serial.println( sensorValue);
  delay(300);

  
 }
